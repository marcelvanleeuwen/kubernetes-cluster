---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: velero
  namespace: velero-system
spec:
  interval: 1m
  chart:
    spec:
      chart: velero
      version: "10.1.1"
      sourceRef:
        kind: HelmRepository
        name: velero
        namespace: velero-system
  interval: 24h
